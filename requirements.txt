# File Finder — Sensitive / Duplicates / Large
# macOS: pip install -r requirements.txt  (then python setup.py py2app, ./build_dmg.sh)
# Windows: pip install -r requirements.txt  (then build_exe.bat)
PyQt5>=5.15
